const User = require("../user");
const mockUser = { id: 1, first: 'Paul', last: 'Cruse', level: 'Black Panther'

// describe("User", () => {
//     describe("constructor", () => {
//         it("user id should be 1", () => {//affirmative
//             const user = new User(mockUser);
//             expect(user.id).toBe(1);
//         });
//         it("user id should not be 3", () => {//negative
//             const user = new User(mockUser);
//             expect(user.id).not.toBe(3);
//         });
//     });

// });